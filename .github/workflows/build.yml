name: Fidimag
on: [push]
jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v2
      - name: "Build and test"
        uses: docker/travis/Dockerfile
        run: |
          make test-docker
          make ipynb-docker
  
    
