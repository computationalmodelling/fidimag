def test_has_pyvtk_installed():
    # use 'pip install pyvtk' if this fails
    import pyvtk

def test_has_fidimag_installed():
    # fix PYTHONPATH if this fails
    import fidimag
    
def test_has_pytest_installed():
    # install pytest if this fails (via conda, apt-get, ...)
    import pytest
